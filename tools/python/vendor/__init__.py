"""Third-party model code vendored into the tree.

Each subpackage carries its upstream LICENSE alongside the code and is
recorded in NOTICE.md. Vendored only where upstream ships no installable
package — see each subpackage's docstring for what, if anything, was changed.
"""
