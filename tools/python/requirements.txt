librosa>=0.10.0
numpy>=1.24.0
scipy>=1.10.0
soundfile>=0.12.0
audioread>=3.0.0
pytest>=7.0.0
requests>=2.28.0
beautifulsoup4>=4.12.0
demucs>=4.0.0
mir_eval>=0.7.0
ruptures>=1.1.7
scikit-learn>=1.3.0
# Pure-Python loudness meter (EBU R128 / BS.1770). Ships wheels for every
# Python/arch combo on PyPI — used by mir_server.py for loudness_lufs.
pyloudnorm>=0.1.1
# Optional power-user dep — not in any Dockerfile. Install locally with
# `pip install essentia` to unlock danceability, dynamic_complexity,
# inharmonicity. mir_server.py gates these features behind a try/except.
